package net.ryzom.zyroom

/**
 * Ce qui sépare les variantes de l'application.
 *
 * Version **dev**, celle du mainteneur : tous les coffres montrent leur
 * contenu, et elle se met à jour comme celle de la guilde — c'est même elle qui
 * essuie les plâtres.
 *
 * Le pendant de ce fichier vit dans `src/guilde/kotlin/` et `src/fdroid/kotlin/`.
 */
const val MASQUE_COFFRES = false

/** Voir `src/guilde/kotlin/` : la dev se met à jour de la même façon. */
const val MISES_A_JOUR_INTEGREES = true
