<?xml version="1.0" encoding="utf-8"?>
<!--
  Le manifeste de la variante F-Droid, fusionné avec celui de `main`.

  Il en retire tout ce qui sert à s'installer soi-même. Ce n'est pas seulement
  une règle de la logithèque : une application qui ne peut pas le faire n'a pas
  à en demander la permission, et un joueur qui lit la liste des permissions
  avant d'installer ne doit pas y trouver « installer des applications » sans
  raison.

  Le fournisseur de fichiers disparaît avec elle : il ne servait qu'à présenter
  l'APK téléchargé au programme d'installation du système.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"
                     tools:node="remove" />

    <application>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            tools:node="remove" />
    </application>
</manifest>
