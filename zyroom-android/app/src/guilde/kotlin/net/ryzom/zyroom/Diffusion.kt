package net.ryzom.zyroom

/**
 * Ce qui sépare les deux variantes de l'application.
 *
 * Version **guilde**, celle qu'on distribue : le contenu du petit coffre n'est
 * pas montré. Le coffre lui-même reste dans la liste, mais vide.
 *
 * Le pendant de ce fichier vit dans `src/dev/kotlin/`, avec la valeur inverse.
 */
const val MASQUE_COFFRES = true
